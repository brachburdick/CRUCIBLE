FROM crucible-runner:base

# Install Rust via rustup for the agent user
USER agent
RUN curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
ENV PATH="/home/agent/.cargo/bin:${PATH}"
USER root
